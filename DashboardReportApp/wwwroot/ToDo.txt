

10.22.25
6. deviation paginate
7. process change paginate


10/27/25 
1. Make print tag option on assembly page

Remove react table. make html table with pagination, datatables and jquery
<table id="maintenanceTable" class="table table-striped table-bordered">


1. pressmixbagchange

Holdtag-add component
x1. In pressrun on insert new record lookup the mix lot and mix code and store in press run

x2. In sinterrun populate the part and component. right now component is not populating
xIn sinterrun get the lotnumber and materialcode from pressrun
In assembly get the lotnumber and materialcode from pressrun
3. On holdtag display the mix lot and mix code from pressrun
4. Seperate quantity of holdtags and quantity of on hold
5. show lot number and materialcode in molding dashboard and does it have to be react?


schedule make search and filter. make graphic showing presses and whats scheduled. include part, component, materialcode, quantity, prodNumber, run

tooling work order
xmake it so you can type in vendor and add to list to choose from.
xprint packing slip when create packing slip is clicked and configure the printer in appsettings.json.
xif fitting/setting choose reference po from open pos for that part.
xallow attaching pictures/videos in new tool work order modal and show preview button in all tool work orders table that when clicked shows the pictures / video in a modal.


xap@sintergy.net group for request po


tooling inv
xmake tool number required

for the create packing slip button show if packing slip was created. make it say either packing slip needs created or packing slip created on <date>. for complete work order button i want it to 
say receive tools instead. I always want the tooling to be marked available if receiving the tools. i need another button that says send out tools that will mark it unavailable. make it say tools sent: <date> if already sent

